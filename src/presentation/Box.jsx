import { unstable_Box } from '@material-ui/core/Box'
// import { style, compose, borders, display, flexbox, palette, positions, shadows, sizing, spacing, typography } from '@material-ui/system'
// import styled from 'styled-components'

// const Box = styled.div`${borders}${display}${flexbox}${palette}${positions}${shadows}${sizing}${spacing}${typography}`

export default unstable_Box